//------------------------------------------------------------------------
// CLabel.h
//------------------------------------------------------------------------
#pragma once
//------------------------------------------------------------------------
#include "../Util/Vectors.h"

//------------------------------------------------------------------------
struct CLabel
{
    std::string text;
    vec2 offset;

    CLabel(
        const std::string& text = "Label",
        const vec2 offset = {0.0f, 0.0f}
    ):
        text(text),
        offset(offset)
    {
    }
};
